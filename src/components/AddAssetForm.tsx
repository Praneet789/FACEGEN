import React from 'react';

// Deprecated component: custom asset addition has been removed.
// Keeping a no-op export to avoid import errors in older builds.
export const AddAssetForm: React.FC = () => null;